# Copy to .env.local and fill in your tunnel URL when running over Cloudflare.
# Leave unset for plain LAN dev (defaults to http://localhost:3001/api/v1).

# Public backend URL the phone's browser will call:
VITE_API_BASE=https://YOUR-BACKEND-TUNNEL.trycloudflare.com/api/v1
